// This page displays the login form
// It allows users to sign into their account

import LoginForm from "@/components/auth/LoginForm";

export default function LoginPage() {
    return (
        <main className="p-6">
            <LoginForm />
        </main>
    );
}